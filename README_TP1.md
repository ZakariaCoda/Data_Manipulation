# TP1 - Manipulation des données

Ce projet est le premier TP de la matière « Manipulation des données ».

Le contenu de ce TP est le suivant :

- créer un environnement virtuel reproductible et figer ses dépendances ;
- créer un repository git pour stocker le projet
- manipuler des tableaux NumPy : slicing, masques booléens, statistiques par axe ;
- mesurer et expliquer l’écart de performance entre une boucle Python et une opération vectorisée.

## Procedure installation

Pour installer ce projet et obtenir la même configuration partout, on doit suivre les étapes suivantes :

1. Récupérer le projet depuis le dépôt Git :
2. Créer puis activer un environnement virtuel :
3. Installer les dépendances du projet pour reproduire la même configuration sur chaque machine :
4. Le fichier `.gitignore` permet d’exclure les fichiers lourds ou générés automatiquement du dépôt Git, notamment pour ne pas partager les données volumineuses à chaque fois.




